{
  "name": "live-classify",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@carbonplan/zarr-layer": "file:vendor/carbonplan-zarr-layer-0.10.0-live-classify.tgz",
    "icechunk-js": "^0.6.0",
    "maplibre-gl": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
